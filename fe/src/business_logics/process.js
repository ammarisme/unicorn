import {array} from "prop-types";
import {Persist} from "./Persist";

export class Process extends Persist {
  constructor() {
    super();
    let operations: array

    this.add_operation = (operation) => {
    }

    this.remove_operation = (operation) => {
    }
  }
}
